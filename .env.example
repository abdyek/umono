APP_ENV=dev # prod
SESSION_DRIVER=db # memory

PORT=8999
DSN=umono.db

# Required. Set both before the first run; Umono hashes them into
# HASHED_USERNAME / HASHED_PASSWORD and clears these two values.
USERNAME=
PASSWORD=
